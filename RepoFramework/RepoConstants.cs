
namespace ErpCore.RepoFramework
{
    public static class RepoConstants
    {
        public enum InfoGroupFilterTypes { Inlcude, Exclude };

        public enum ObjectStates
        {
            Unchanged,
            Added,
            Modified,
            Deleted
        }
    }
}
